<%= form_for @tipo_evento, :html => { :class => 'form-horizontal' } do |f| %>
  <div class="form-group">
    <%= f.label :descricao, :class => 'control-label' %>
    <%= f.text_field :descricao, :class => 'form-control' %>
  </div>
  <div class="form-group">
    <%= f.label :acao, :class => 'control-label' %>
    <%= f.select :acao, [['Movimento',1],['Parado',0]],{},{:class => 'form-control'} %>
  </div>

  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                tipo_eventos_path, :class => 'btn' %>
  </div>
<% end %>
